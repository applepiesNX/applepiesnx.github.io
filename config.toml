# The URL the site will be built for
base_url = "https://applepiesnx.github.io"
title = "applepiesNX's Blog"
# Whether to automatically compile all Sass files in the sass directory
compile_sass = true
highlight_theme = "solarized-light"

theme="blow"



build_search_index = true
taxonomies = [
    {name = "tags", paginate_by = 5, feed = true},
    {name = "categories", paginate_by = 5, feed = true},
]
[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "base16-ocean-dark"
render_emoji = true
[search]
# Whether to include the title of the page/section in the index
include_title = true
# Whether to include the description of the page/section in the index
include_description = true
# Whether to include the path of the page/section in the index
include_path = true
# Whether to include the rendered content of the page/section in the index
include_content = true
# At which character to truncate the content to. Useful if you have a lot of pages and the index would
# become too big to load on the site. Defaults to not being set.
truncate_content_length = 100

# Optional translation object for the default language
# Example:
#     default_language = "fr"
#
#     [translations]
#     title = "Un titre"
[extra]
[extra.index]
title = "Praveen N"
image = "https://via.placeholder.com/200"
[extra.sidebar]
items = [
  { lang = "en", links = [
        { url = "/", name = "Home" },
        { url = "/categories", name = "Categories" },
        { url = "/tags", name = "Tags" },
        { url = "/blog", name = "Blog" },
    ] },
]
[extra.navbar]
items = [
    { lang = "en", links = [
        { url = "/", name = "Home" },
        { url = "/categories", name = "Categories" },
        { url = "/tags", name = "Tags" },
        { url = "/blog", name = "Blog" },
    ] },
]
title = "applepiesNX's Blog"
[extra.social]
github = "https://github.com/applepiesnx"
gitlab = ""
twitter = ""
linkedin = "https://www.linkedin.com/in/praveennawarathne/"
email = ""
[extra.lang]
items = [
    { lang = "en", links = [
        { base_url = "/", name = "English" },
    ] },
]
[extra.default_author]
name = "Praveen N"
avatar = "https://via.placeholder.com/200"
